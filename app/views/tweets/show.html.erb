<p><strong>内容</strong>
<%= @tweets.message %></p>
<% if @tweets.file != nil %>
    <%= image_tag "/get_image/#{tweet.id}" %>
<% end %>
<p><strong>時間</strong>
<%= ((Time.current - @tweets.tdate)/60).floor %>分前
</p>

<%= link_to 'back', '/' %>